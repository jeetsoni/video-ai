{
  "name": "video-ai",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "turbo": "^2.9.0"
  },
  "packageManager": "npm@11.12.1",
  "engines": {
    "node": ">=24"
  }
}
